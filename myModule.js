// write a js program to convert the sentence into upper and lower case.

const capitalize = (text) => text.toUpperCase();

const lowerize = (text) => text.toLowerCase();

// console.log(lowerize("FHKJFSJKGKSG"))

module.exports = {capitalize, lowerize} ;
